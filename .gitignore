# ── Node.js dependencies ──────────────────────────────────────
# This folder is auto-generated when you run "npm install"
# It can contain thousands of files — never push it to GitHub
node_modules/

# ── Local environment variables ───────────────────────────────
# This file stores secret passwords for local development
# It must NEVER be pushed to GitHub — keep it private
.env